pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello from me............!'
                sh 'ls -ltr'
                echo 'End of code'
                echo "Hello second time"
            }
        }
    }
}
